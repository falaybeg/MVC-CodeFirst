@model IEnumerable<EntityFrameworkExample.Models.Product>

@{
    ViewBag.Title = "liste";
}
<center>
    <h2 style="background-color: dimgray; color:white">Product List</h2>
</center><hr />

<p>
    <a href="@Url.Action("AddProduct","Index")" class="btn btn-primary"><b>New Product</b></a>

</p>

<table class="table table-hover table-bordered col-md-10">

    <thead class="thead-inverse">
        <tr>

            <th>
               
            </th>
            <th>
                <label>Stock Amount</label>
            </th>
            <th>
                <label>Category Name</label>
            </th>
            <th>
                <label>Supplier Name</label>
            </th>
            <th></th>
        </tr>
    </thead>
    @foreach (var item in Model)
            {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Stock)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Category.CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supplier.Name)
            </td>
            <td>
                <a href="@Url.Action()">Edit</a>
                <a href="">Delete</a>

                @Html.ActionLink("Edit", "Edit", new { id = item.Id, @class = "btn btn-primary" }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>

